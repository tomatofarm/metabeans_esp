# 백엔드 base URL — API_BASE_PATH 포함 (끝에 슬래시 없음)
# 예: http://localhost:3000/api/v1
VITE_API_BASE_URL=

# true 이면 Mock API만 사용 (VITE_API_BASE_URL 이 있어도)
# VITE_USE_MOCK_API=true

# true 이면 브라우저 콘솔에 실제 HTTP 요청/응답 로그 (메서드 + 전체 URL, apiClient 사용 시만)
# VITE_DEBUG_API=true
