# UI 개선 프롬프트 #07 — 시스템 관리

## ⚠️ 선행 조건
UI_00_공통기반 프롬프트 실행 완료 후 진행.

## 작업 범위
- `SystemPage.tsx` (4개 탭)
  - 탭 1: 역할/권한 관리
  - 탭 2: 가입 승인
  - 탭 3: 사용자 관리
  - 탭 4: 기준 수치 설정

## 레퍼런스
`docs/ESP_시스템관리__1_.html` 읽고 참고할 것.

---

## 1. 탭 메뉴 (시스템 관리 4탭)

```
탭 컨테이너:
  background white, border-radius 12px
  padding 8px, box-shadow var(--card-shadow), margin-bottom 24px
  display flex, gap 8px

각 탭: UI_03_장비관리와 동일 스타일 (padding 10px 20px, border-radius 8px)
```

---

## 2. 가입 승인 탭

승인 대기 목록 카드:
```
각 신청 카드:
  background white, border-radius 12px
  padding 20px, box-shadow var(--card-shadow), margin-bottom 16px
  border-left 4px solid var(--color-warning)  ← 승인대기
  display flex, align-items center, gap 16px

  [좌] 역할 배지 원형 (40x40)
       OWNER:  background linear-gradient(135deg, #4A6CF7, #6B7CFF)
       HQ:     background linear-gradient(135deg, #10B981, #34D399)
       DEALER: background linear-gradient(135deg, #F59E0B, #FBBF24)
       color white, font-size 0.7rem, font-weight 700, border-radius 50%

  [중] 상호명 (font-weight 700) + 사업자번호 + 신청일시
       연락처 / 담당자명 (font-size 0.85rem, color var(--color-mid))

  [우] 버튼 2개:
       승인: background var(--color-success), color white
             padding 8px 16px, border-radius 8px, font-weight 600
       반려: background var(--color-danger-bg), color var(--color-danger-text)
             padding 8px 16px, border-radius 8px, font-weight 600
```

승인 처리 완료 카드:
```
border-left-color var(--color-success)  ← 활성
border-left-color #94A3B8               ← 반려
```

---

## 3. 사용자 관리 탭

테이블: UI_05 A/S 관리와 동일한 테이블 스타일 적용.

사용자 상태 배지:
```
활성:    StatusBadge success
비활성:  StatusBadge default
승인대기: StatusBadge warning
```

역할 배지:
```
ADMIN:  background #EEF2FF, color var(--color-primary)
DEALER: background var(--color-warning-bg), color var(--color-warning-text)
HQ:     background var(--color-success-bg), color var(--color-success-text)
OWNER:  background #F3E8FF, color #7C3AED
pill 형태
```

---

## 4. 기준 수치 설정 탭 (threshold)

기준 수치 카드 그리드:
```
레이아웃: display grid, grid-template-columns repeat(3, 1fr), gap 20px

각 기준 수치 카드:
  background white, border-radius 16px
  padding 24px, box-shadow var(--card-shadow)

카드 헤더:
  display flex, align-items center, gap 12px, margin-bottom 20px

  아이콘 박스 (40x40, border-radius 10px):
    스파크:   linear-gradient(135deg, #F59E0B, #FBBF24)
    차압:     linear-gradient(135deg, #6366F1, #818CF8)
    변화율:   linear-gradient(135deg, #10B981, #34D399)
    유입온도: linear-gradient(135deg, #EF4444, #F87171)
    PM2.5:   linear-gradient(135deg, #F97316, #FB923C)
    PM10:    linear-gradient(135deg, #EC4899, #F472B6)

  제목 영역:
    카드명: font-size 1rem, font-weight 700
    설명:   font-size 0.8rem, color var(--color-mid)

카드 본문 (수치 입력 그룹):
  각 임계값 행:
    margin-bottom 16px

    라벨:
      font-size 0.85rem, color var(--color-mid), font-weight 500
      margin-bottom 8px

    입력 래퍼 (display flex, align-items center):
      인풋:
        flex 1, padding 10px 14px
        border 1.5px solid var(--color-border), border-radius 8px 0 0 8px
        font-size 0.95rem
        focus: border-color var(--color-primary), 파란 글로우

      단위 박스:
        padding 10px 14px
        background var(--color-bg), border 1.5px solid var(--color-border)
        border-left none, border-radius 0 8px 8px 0
        font-size 0.85rem, color var(--color-mid)
```

저장 버튼:
```
위치: 각 카드 하단 또는 페이지 하단 고정
primary 버튼 스타일 (padding 12px 32px)
저장 성공 시: 성공 토스트 메시지
```

토스트 메시지:
```
position fixed, bottom 24px, right 24px, z-index 9999
background var(--color-dark), color white
padding 14px 20px, border-radius 10px
box-shadow 0 8px 24px rgba(0,0,0,0.2)
display flex, align-items center, gap 10px
animation: slideInRight 0.3s ease

성공: 좌측 초록 dot + 텍스트
실패: 좌측 빨간 dot + 텍스트
```

---

## 5. 역할/권한 관리 탭

권한 매트릭스 테이블:
```
헤더 행: 역할별 (ADMIN / DEALER / HQ / OWNER)
기능 행: 각 기능명 + 역할별 허용 여부 (체크박스 또는 아이콘)

허용: ✅ (초록 아이콘, color var(--color-success))
불가: ❌ (회색 아이콘, color var(--color-light))

테이블 스타일: UI_05 A/S 관리 테이블과 동일
```

---

## 완료 기준
- [ ] 가입 승인 카드: 역할별 컬러 아이콘 + 좌측 보더 색상
- [ ] 승인/반려 버튼 색상 구분
- [ ] 사용자 역할 배지 4종 색상 구분
- [ ] 기준 수치 카드: 아이콘 그라데이션 + 단위 박스 인풋
- [ ] 저장 성공 시 토스트 메시지
- [ ] 기능(승인 처리, 수치 저장)에 영향 없음
